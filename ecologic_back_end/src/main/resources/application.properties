# ===============================================================
# CONFIGURACAO DO BANCO DE DADOS (SUPABASE / POSTGRESQL)
# ===============================================================

spring.datasource.url=jdbc:postgresql://db.vkffpxctoszpdykhjxse.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=EcoLogic123!
spring.datasource.driver-class-name=org.postgresql.Driver

# ===============================================================
# CONFIGURACOES DO JPA / HIBERNATE
# ===============================================================

# 'update' cria ou atualiza as tabelas automaticamente na primeira inicializacao
spring.jpa.hibernate.ddl-auto=update

# Mostra o SQL gerado no console do IntelliJ
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialeto correto para o PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ===============================================================
# CONFIGURACAO DO SERVIDOR
# ===============================================================

# Porta que vai rodar localmente
server.port=8080

# ===============================================================
# CONFIGURACAO DE ENVIO DE E-MAIL (MAILTRAP - TESTES)
# ===============================================================

spring.mail.host=sandbox.smtp.mailtrap.io
spring.mail.port=587
spring.mail.username=8285868e91bd65
spring.mail.password=ece36b373decee
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.mail.from=noreply@ecologic.com.br
app.frontend.url=http://localhost:5173